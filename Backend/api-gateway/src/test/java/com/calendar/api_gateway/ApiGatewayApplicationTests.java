package com.calendar.api_gateway;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest // Indicates that this is a Spring Boot test, which loads the full application context
class ApiGatewayApplicationTests {

    @Test // Marks this method as a test case
    void contextLoads() {
        // This test checks if the Spring application context loads successfully.
        // If the context fails to load, this test will fail, indicating a configuration issue.
    }
}

